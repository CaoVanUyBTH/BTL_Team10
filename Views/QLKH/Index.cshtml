@model IEnumerable<Nhom10.Models.QLKH>
@{
    ViewData["Title"] = "Index";
}
<h1>Index</h1>
<p> 
    <a asp-action="Create">Creat New</a> | <a asp-action="Upload">Input Excel</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QLKHID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QLKHName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.QLKHID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.QLKHName)
                </td>

                <td>
                    <a asp-action="Edit" asp-route-id="@item.QLKHID">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.QLKHID">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.QLKHID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>